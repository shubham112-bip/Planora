# =====================================================================
# Production runtime dependencies
# Pinned to known-good versions; bump deliberately, not accidentally.
# =====================================================================

# --- Editable installs (paths resolved from apps/api/ cwd) -----------
# Domain layout: every capability is its own editable package under packages/.
# (Phase folders no longer exist; see `docs/implementationPlan.md` for the
# phase-to-package mapping.)
-e ../../packages/platform
-e ../../packages/domain_contracts
-e ../../packages/orchestrator
-e ../../packages/tools
-e ../../packages/agents/destination
-e ../../packages/agents/logistics
-e ../../packages/agents/budget
-e ../../packages/agents/synthesis
-e ../../packages/agents/validator
-e ../../packages/agents/repair
-e ../../packages/memory
-e ../../packages/reliability

# --- Web / API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.3

# --- Settings / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- LLM / agent frameworks (used from Phase 1 onward) ---
langgraph==0.2.34
langchain==0.3.1
langchain-core==0.3.7
langchain-openai==0.2.1
langchain-anthropic==0.2.1
openai==1.51.0
anthropic==0.34.2

# --- Tools / IO (Phase 2+) ---
httpx==0.27.2
tenacity==8.5.0
tavily-python==0.5.0

# --- Memory / state (Phase 7+) ---
redis==5.0.8
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.3
chromadb==0.5.5

# --- Logging / observability ---
structlog==24.4.0
python-json-logger==2.0.7

# --- Utilities ---
python-dotenv==1.0.1
typer==0.12.5
